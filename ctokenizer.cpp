//
// Created by Ethan Edwards on 9/4/24.
//

#include "ctokenizer.h"

std::vector<ctoken> ctokenizer::tokenize(std::ifstream& in) {
    std::vector<ctoken> tokens;
    while (!in.eof()) {
        // ctoken token;
        // tokens.push_back(token);
    }
    return tokens;
}